services:
  - type: web
    name: ai-news-hub
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    nodeVersion: "20.x"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
